set -g default-shell "/bin/zsh" 

# set-option -sa terminal-overrides ",xterm*:Tc"
set -g mouse on

# Change leader (turned off because it interfeers with vimwiki's todo lists)
# unbind C-b
# set -g prefix C-Space
# bind C-Space send-prefix

# Start windows and panes at 1, not 0
set -g base-index 1
set -g pane-base-index 1
set-window-option -g pane-base-index 1
set-option -g renumber-windows on

# Use Alt-arrow keys without prefix key to switch panes
bind -n M-Left select-pane -L
bind -n M-Right select-pane -R
bind -n M-Up select-pane -U
bind -n M-Down select-pane -D

# Shift arrow to switch windows
bind -n S-Left  previous-window
bind -n S-Right next-window

# Shift Alt vim keys to switch windows
bind -n M-H previous-window
bind -n M-L next-window

# Uncomment to change theme flavor
# set -g @catppuccin_flavour 'mocha'

# leader+[/] to switch windows
bind [ previous-window
bind ] next-window

# Use bindings similar to vim to split panes
bind s split-window -v -c "#{pane_current_path}"
bind v split-window -h -c "#{pane_current_path}"


# iTerm special keybinding to allow shift-enter to send a special code
# bind -n S-Enter send-keys Escape "[13;2u"

set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-sensible'
set -g @plugin 'christoomey/vim-tmux-navigator'
set -g @plugin 'tmux-plugins/tmux-yank'
set -g @plugin 'tmux-plugins/tmux-resurrect'

# Initialize TMUX plugin manager (keep this line at the very bottom of tmux.conf)
run '~/.tmux/plugins/tpm/tpm'
